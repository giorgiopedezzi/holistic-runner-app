# Generate User Stories

Read and follow exactly:

`.agents/workflows/refine-prompt.md`

Active harness identity: `Claude Code`.

If given a Jira Research/Spike labeled `ai-prompt`, treat it as the immutable source artifact defined by the shared workflow.

If given untracked requirement text instead, use the workflow's untracked-requirement mode.

Do not duplicate or reinterpret the shared workflow here.

Never bypass its human clarification or Jira-creation approval gates.
