# Loaded via `node --env-file=.env.test` for `npm test` (see package.json).
# Tests use an in-memory SQLite DB (test/helpers/db.ts) and never read this
# path — DB_PATH just needs to be set so loadConfig() doesn't throw at boot.
DB_PATH=:memory:
