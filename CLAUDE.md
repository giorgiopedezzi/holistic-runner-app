@AGENTS.md

# Claude Code adapter

- For Story execution, use `/implement-story`.
- `.claude/rules/*.md` are native Claude Code path-scoped rules; let them load normally.
- The shared `AGENTS.md` is the global source of truth. Do not duplicate shared rules here.
